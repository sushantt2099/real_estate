<% if @property.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-error">
			The Foam contains <%= pluralize(@property.errors.count, "error") %>
		</div>
		<ul>
			<% @property.errors.full_messages.each do |e| %>
				<li>* <%= e %></li>
			<% end %>
		</ul>
	</div>
<% end %>